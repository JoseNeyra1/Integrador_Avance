# Copia este archivo como ".env" y ajusta los valores. NUNCA subas el ".env" real al repo.

# Base de datos
DB_NAME=TiendaFlic_db
DB_PASSWORD=elige_una_contraseña_segura

# Orígenes permitidos para CORS (la URL donde vive tu frontend), separados por coma
CORS_ALLOWED_ORIGINS=http://localhost:5500,https://tu-frontend.vercel.app
